package serie3;

public class GraphUtils {

	public static int numberOfNeighoursAtDistanceN(Vertex[] graph, int verticeId, int n){
		if (n<0)
			return -1;
		boolean aux [] = new boolean[graph.length];
		int count=0;
		
		return count;
	}
}
